<!-- 質問に紐づくtag_listの場合は、tag_nameが引き出される。タグ絞り込み検索の際にtag_nameがparamsに引き渡される -->
<div>
<% tag_list.each do |tag| %>
	<%= link_to questions_path(tag_name: tag), class:"link-tag" do %>
  		<span class="tag label label-info"><%= tag %></span>
  	<% end %>
<% end %>
</div>
